# ================================
# 创意工作室应用环境变量配置
# ================================
# 复制此文件为 .env 并修改配置值
# cp .env.example .env

# ================================
# 必需配置 (Required)
# ================================

# 应用端口
PORT=3006

# 运行环境: development | production
NODE_ENV=production

# 管理员账户 (请修改默认值!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_me_to_secure_password

# ================================
# 可选配置 (Optional)
# ================================

# 应用名称
APP_NAME=Creative Studio

# 日志级别: debug | info | warn | error
LOG_LEVEL=info

# 上传文件大小限制 (字节)
UPLOAD_MAX_SIZE=10485760

# 允许的上传文件类型
UPLOAD_ALLOWED_TYPES=jpg,jpeg,png,gif,webp,svg,pdf

# ================================
# 安全配置 (Security)
# ================================

# 允许的域名 (CORS)
ALLOWED_ORIGINS=http://localhost

# 速率限制 - 时间窗口 (毫秒)
RATE_LIMIT_WINDOW=900000

# 速率限制 - 最大请求数
RATE_LIMIT_MAX_REQUESTS=100

# ================================
# Docker 配置
# ================================

# 容器资源限制
APP_CPU_LIMIT=1.0
APP_MEMORY_LIMIT=512M
NGINX_CPU_LIMIT=0.5
NGINX_MEMORY_LIMIT=128M

# ================================
# 注意事项
# ================================
# 1. 生产环境必须修改 ADMIN_PASSWORD
# 2. 敏感信息不要提交到版本控制
# 3. 使用强密码 (至少8位,包含字母和符号)

# ================================
# 邮件服务配置 (Email Service)
# ================================
# 用于发送邮箱验证码

# SMTP 服务器地址
SMTP_HOST=smtp.gmail.com

# SMTP 端口 (587=TLS, 465=SSL)
SMTP_PORT=587

# 是否使用 SSL (true/false)
SMTP_SECURE=false

# SMTP 用户名 (通常是邮箱地址)
SMTP_USER=your-email@gmail.com

# SMTP 密码/应用专用密码
# Gmail 需要使用应用专用密码: https://myaccount.google.com/apppasswords
SMTP_PASS=your-app-password

# 发件人地址 (可选, 默认使用 SMTP_USER)
EMAIL_FROM=your-email@gmail.com
